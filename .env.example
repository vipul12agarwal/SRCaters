# Copy to .env.local for local testing, or set in Vercel → Settings → Environment Variables.
# Use your deployment URL (no trailing slash). Update when you add a custom domain.
NEXT_PUBLIC_SITE_URL=https://your-project.vercel.app
